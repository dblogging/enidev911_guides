## webbrowser  

Este módulo proporciona interfaz de alto nivel que permite mostrar documentos basados en la web a los usaurios. En la mayoría de los casos, simplemente llamar a la función **open()** desde este módulo hará lo correcto.

El script **webbrowser** puede ser usado como una interfaz de línea de comandos, acepta una URL como argumento. Acepta los siguientes parámetros opcionales: 

- `-n` : abre la URL en una nueva ventana del navegador (si es posible)
- `-t` : abre la URL en una nueva página del navegador.

Ejemplos de uso: 

```bash
python -m webbrowser -t 'https://www.python.org'
```